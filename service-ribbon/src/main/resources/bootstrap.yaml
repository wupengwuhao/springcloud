spring:
  application:
    name: service-ribbon
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        serviceId: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://10.31.0.234:7070/eureka/
